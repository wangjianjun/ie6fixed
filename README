
ie6fixed solve the problem that IE6 browser does not support the fixed position, at the same time avoid flicker when the window is scrolling.

ie6fixed provide two versions, one is common version, another is a seajs module .

ie6fixed depend on jquery.